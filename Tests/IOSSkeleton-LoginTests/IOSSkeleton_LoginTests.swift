    import XCTest
    @testable import IOSSkeleton_Login

    final class IOSSkeleton_LoginTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(IOSSkeleton_Login().text, "Hello, World!")
        }
    }
